# Docker Compose 会自动读取同目录下的 .env（如果存在）。
# 你可以复制本文件为 .env，然后按需修改。

# 对外只暴露一个端口（宿主机端口 -> 容器内 8080）
HOST_PORT=8080

# 单端口方案对外基地址（用于生成“打开”链接、启动日志 url=...、API 返回 access_url）
STREAMLIT_HOST_PUBLIC_BASE=http://127.0.0.1:8080

# （可选）管理台内部调用 API 的地址
STREAMLIT_HOST_API_URL=http://127.0.0.1:8080/api


